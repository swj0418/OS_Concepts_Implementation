package testsite;
import process.*;
import scheduler.*;

public class Test {
	public static void main(String[] ar) {
		Process_Ex p = new Process_Ex();
		Scheduler s = new Scheduler();
		
		
		p.Job_Ex();
		System.out.println(p.my_PCB.accounting_info.init_time);
	}
}
